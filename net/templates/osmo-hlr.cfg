${include(common_template_warning)}
hlr
 database hlr.db
 gsup
  bind ip ${HLR_IP}
  ipa-name HLR-db
 ussd route prefix *0# internal own-msisdn
 ussd route prefix *1# internal own-imsi
 ussd route prefix *#100# internal own-msisdn
 ussd route prefix *#101# internal own-imsi
 store-imei

mslookup
 mdns
 server
  msc ${MSC_IPA_NAME}
   service sip.voice at ${SIPCON_SIP} ${SIPCON_SIP_PORT}
   service smpp.sms at ${MSC_SMPP_IP} 2775
  msc ${MSC2_IPA_NAME}
   service sip.voice at ${SIPCON2_SIP} ${SIPCON2_SIP_PORT}
   service smpp.sms at ${MSC2_SMPP_IP} 2775

line vty
 bind ${HLR_VTY_CTRL_IP}
ctrl
 bind ${HLR_VTY_CTRL_IP}

${foreach(LOG_OUTPUT)}
log ${LOG_OUTPUTn_TYPE}
${include(common_logging)}
 logging level linp error
${foreach_end}

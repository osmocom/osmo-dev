${include(common_template_warning)}
hlr
 database hlr-proxy-proxy.db
 gsup
  bind ip ${HLRPROXY_PROXY_IP}
  ipa-name HLR-proxy-proxy
 ussd route prefix *0# internal own-msisdn
 ussd route prefix *1# internal own-imsi
 ussd route prefix *#100# internal own-msisdn
 ussd route prefix *#101# internal own-imsi
 store-imei

line vty
 bind ${HLRPROXY_PROXY_IP}
ctrl
 bind ${HLRPROXY_PROXY_IP}

mslookup
 server
  mdns
  service sip.voice at ${SIPCON_LOCAL} ${SIPCON_LOCAL_PORT}
 client
  gateway-proxy ${HLRPROXY_IP}

${foreach(LOG_OUTPUT)}
log ${LOG_OUTPUTn_TYPE}
${include(common_logging)}
 logging level linp error
${foreach_end}

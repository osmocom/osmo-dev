${include(common_template_warning)}
line vty
 bind ${MSC2_VTY_CTRL_IP}
app
mncc
  socket-path ${MSC2_MNCC_SOCKET}
sip
  local ${SIPCON2_SIP} ${SIPCON2_SIP_PORT}
  remote ${PBX_SIP} ${PBX_SIP_PORT}

${foreach(LOG_OUTPUT)}
log ${LOG_OUTPUTn_TYPE}
${include(common_logging)}
${foreach_end}
